@model ScoreMe.UI.Models.EnumVM
@using PagedList.Mvc;

@{
    ViewBag.Title = "Index";
    Layout = "";
}




<div class="col-sm-1 col-sm-offset-11" style="position: absolute;">
    <span class="label label-danger pull-right badgemargin">@Model.ListCount</span>
</div>

<table class="table table-bordered">
    <tbody>
        @foreach (var item in Model.REnumValueList)
        {
        <tr>
            <td class="col-md-2">
                @Html.DisplayFor(modelItem => item.EnumCategoryName)
            </td>
            <td class="col-md-2">
                @Html.DisplayFor(modelItem => item.EnumValueCode)
            </td>
            <td class="col-md-2">
                @Html.DisplayFor(modelItem => item.EnumValueName)
            </td>

            <td class="col-md-2">
                @Html.DisplayFor(modelItem => item.EnumValueDesc)
            </td>


            <td class="col-md-1 shsingle text-center" style="font-size:16px;">
                <a class="btn btn-xs" title="Redaktə et" href="@Url.Action("Edit","EnumValue", new { id = item.EnumValueID})"><span class="fa fa-pencil"></span></a>
                <span title="Sil"> @Html.ActionLink(" ", "Delete", "EnumValue", new { id = item.EnumValueID }, new { @onclick = "return confirm('Silmək istədiyinizə əminsinizmi?.');", @class = "btn btn-xs glyphicon glyphicon-trash" })</span>
            </td>

        </tr>
        }
    </tbody>
</table>


<div class="box-footer clearfix">
    Səhifə @(Model.Paging.PageCount < Model.Paging.PageNumber ? 0 : Model.Paging.PageNumber) (cəmi @Model.Paging.PageCount)
    @Html.PagedListPager(Model.Paging, page => Url.Action("Index", new { page }))
</div>






